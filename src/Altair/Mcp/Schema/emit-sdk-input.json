{
    "type": "object",
    "additionalProperties": false,
    "required": ["language"],
    "properties": {
        "language": {
            "type": "string",
            "enum": ["typescript", "python"],
            "description": "Target SDK language."
        }
    }
}
