{
    "type": "object",
    "additionalProperties": false,
    "required": ["interface"],
    "properties": {
        "interface": {
            "type": "string",
            "description": "Fully-qualified interface name."
        }
    }
}
