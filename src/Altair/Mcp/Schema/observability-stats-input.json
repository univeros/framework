{
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100000,
            "description": "How many rows back to read (default 5000)."
        }
    }
}
