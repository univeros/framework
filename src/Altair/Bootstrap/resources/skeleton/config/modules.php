<?php

declare(strict_types=1);

/*
 * Modules installed in this app. Each entry is a `ModuleInterface` instance;
 * registering one wires its routes, Cycle entities and migrations into the app
 * automatically — no other file needs to change.
 *
 * Example:
 *   return [
 *       new Acme\UserManagement\Module(),
 *   ];
 *
 * Scaffold your own with `bin/altair module:new --name=acme/your-module`.
 *
 * @return list<Altair\Module\Contracts\ModuleInterface>
 */
return [];
