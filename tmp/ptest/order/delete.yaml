endpoint:
  method: DELETE
  path: '/store/order/{orderId}'
  summary: 'Delete purchase order by identifier.'
  tags:
    - order
input:
  orderId:
    type: int
    in: path
    rules:
      - required
domain:
  class: App\Order\DeleteOrder
  invocation: __invoke
