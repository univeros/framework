endpoint:
  method: GET
  path: /user/login
  summary: 'Logs user into the system.'
  tags:
    - login
input:
  username:
    type: string
    in: query
    rules: {  }
  password:
    type: string
    in: query
    rules: {  }
output:
  200:
    body:
      result: string
domain:
  class: App\Login\LoginUser
  invocation: __invoke
