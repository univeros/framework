endpoint:
  method: DELETE
  path: '/user/{username}'
  summary: 'Delete user resource.'
  tags:
    - user
input:
  username:
    type: string
    in: path
    rules:
      - required
domain:
  class: App\User\DeleteUser
  invocation: __invoke
