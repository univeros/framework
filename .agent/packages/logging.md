# univeros/logging  ·  Altair\Logging

**Purpose:** PSR-3 application logging for Altair, backed by Monolog and wired from LOG_* env vars. JSON-lines to stderr by default.

## Concrete classes

- `LoggingConfiguration` _(final)_ — implements `ConfigurationInterface`
- `LoggingSettings` _(final)_

## Tests as documentation

- `tests/Logging/LoggingConfigurationTest.php`
- `tests/Logging/LoggingSettingsTest.php`

## Related packages

- `monolog/monolog`
- `psr/log`
- `univeros/configuration`
- `univeros/container`
